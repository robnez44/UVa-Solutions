#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, N, K, P, i = 1;
    cin >> t;

    while (cin >> N >> K >> P)
    {
        if (N == P)
            cout << "Case " << i << ": " << K << "\n";
        else
        {
            /* code */
        }
        ++i;
    }

    return 0;
}